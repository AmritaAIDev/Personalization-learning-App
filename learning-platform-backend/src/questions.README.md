# Question bank and review workflow

Learners receive only `PUBLISHED` question content without answer keys through
the public catalog and practice APIs. AI generation is restricted to admins and
persists as `DRAFT`; a reviewer must explicitly publish or archive it. The
admin review endpoint is role-protected and is the only list endpoint that
returns answer keys and solutions for draft records.
