# Question bank and review workflow

Learners only ever receive `PUBLISHED` question content, with no answer keys, through the public catalog and practice APIs. AI generation is admin-only and persists as `DRAFT` until a reviewer explicitly publishes or archives it. The admin review endpoint is the only one that returns answer keys/solutions for drafts.
